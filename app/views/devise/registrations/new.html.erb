<%= render 'global/navbar' %>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

<div class="createAccount row">
  <div class="col s12 m6 offset-m3">
  <div class="input-field">
    <i class="material-icons prefix">account_circle</i>
    <label for="icon_prefix">Full Name</label>
    <%= f.text_field :fullname, autofocus: true %>
  </div>

  <div class="input-field">
    <i class="material-icons prefix">email</i>
    <label for="icon_prefix">Email Address</label>
    <%= f.email_field :email %>
  </div>

  <div class="input-field">
    <i class="material-icons prefix">lock_open</i>
    <label for="icon_prefix">Password</label>
    <%= f.password_field :password, autocomplete: "off" %>
  </div>

  <div class="input-field">
    <i class="material-icons prefix">phone</i>
    <label for="icon_prefix">Phone Number</label>
    <%= f.text_field :phone_number %>
  </div>

  <div class="input-field">
    <i class="material-icons prefix">perm_identity</i>
    <label for="icon_prefix">Profile Picture</label>
    <%= f.text_field :file %>
  </div>

  <div class="privacy">
    <p>By signing up, I agree to Find My Draft’s <a href="#">Terms of Service</a>, <a href="#">Privacy Policy</a>, and <a href="#">Venue Guarantee Terms</a>.</p>
    <br>
  </div>

  <div class="actions">
    <%= f.submit "Create Account", :class=>'waves-effect waves-blue waves-ripple btn createBtn blue-grey darken-2"' %>
  </div>
  <% end %>
  </div>
</div>

<%= render "devise/shared/links" %>
